import "tfplan/v2" as tfplan

no_updates_or_destroys = rule {
  all tfplan.resource_changes as _, rc {
    rc.change.actions not in ["update", "destroy"]
  }
}

main = rule {
  no_updates_or_destroys
}
