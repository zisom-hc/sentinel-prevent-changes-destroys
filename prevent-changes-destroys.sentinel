import "tfplan/v2" as tfplan

main = rule {
  
  update := tfplan.resource_changes[_].change.actions[_] contains "update"
  destroy := tfplan.resource_changes[_].change.actions[_] contains "destroy"
  
  update or destroy
}
